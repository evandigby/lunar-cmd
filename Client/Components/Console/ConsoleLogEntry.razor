@using Data.Log

@if (LogEntry is PlaintextLogEntry plaintextLogEntry)
{
    <PlaintextConsoleLogEntry LogEntry=plaintextLogEntry />
}

@code {
    [Parameter]
    public LogEntry LogEntry { get; set; }
}
